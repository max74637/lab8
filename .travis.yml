sudo: required
language: cpp
os:
  - linux
compiler: 
  - g++

before_install:
  - sudo apt-get install libssl-dev
  - sh scripts/install-libprogbase.sh

script: 
  - qmake -o Release/Makefile
  - cd Release
  - make
  - cd ..

